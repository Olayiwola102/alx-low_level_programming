#include "main.h"

/**
 * mul - multiplication of two numbers
 * @a: int value to multiply b
 * @b: int value to multiply a
 * Return: the multiplication result after operation
 */
int mul(int a, int b)
{
	return (a * b);
}
